# Python runtime dependency note
# PyGObject and GTK4/libadwaita bindings are system packages on Ubuntu.
# Install them with:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1
#
# The install.sh script handles this automatically.
#
# No additional pip packages are required.
PyGObject>=3.42
